export enum CouponType {
  PERCENTAGE = "percentage",
  FLAT = "flat",
  FREE_SHIPPING = "free_shipping",
}

export enum CouponStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
  EXPIRED = "expired",
  ARCHIVED = "archived",
}
